# Application
spring.application.name=hidden-ghana-backend
server.port=8080

# Database (Neon Postgres)
spring.datasource.url=jdbc:postgresql://ep-shiny-cloud-zadglj59-pooler.c-2.eu-west-2.aws.neon.tech/hiddenghana?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_hZ8Bm2nUXAtV

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.initialization-fail-timeout=20000

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT - Base64 encoded secret
jwt.secret=${JWT_SECRET:aGlkZGVuZ2hhbmFfc2VjcmV0X2tleV8yMDI2X3ZlcnlfbG9uZ19zZWNyZXRfa2V5X2Zvcl9qd3RfdG9rZW4=}
jwt.expiration=${JWT_EXPIRATION_MS:86400000}

# Optional OAuth client configuration
# For Google/Apple social sign-in you should set the client IDs used by your apps.
# Example:
# apple.client.id=com.example.hiddenghana
# google.client.id=your-google-client-id.apps.googleusercontent.com

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.hiddenghana=DEBUG

# Email (SMTP) — override with MAIL_USERNAME / MAIL_PASSWORD env vars
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-16-character-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Used to build the email verification link.
# Must be reachable from the device that opens the email. Defaults to the LAN
# IP the mobile app uses; override at runtime with APP_BASE_URL if needed.
app.base-url=${APP_BASE_URL:http://10.246.131.61:8080}